#include "main.h"

/**
 * is_prime_number - function that prints prime num
 * @n: parameter
 * Return: 1 if is a prime number and 0 if otherwise
 */

int is_prime_number(int n)
{
	if (n % 10 == 1)
		return (1);
	return (0);
}
